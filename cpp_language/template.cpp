#include <iostream>
using namespace std;

template<typename It>
void print_all(It begin, It end) {
    for (auto it = begin; it != end; ++it)
        cout << *it << " ";
}

template<typename T>


int main() {
    
    return 0;
}